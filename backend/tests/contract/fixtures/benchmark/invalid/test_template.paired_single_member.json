{
  "kind": "test_template",
  "schema_version": "benchmark_test_template_v1",
  "template_id": "paired_invalid_v1",
  "template_version": "1.0.0",
  "operation": "chat_completion",
  "stages": [
    {
      "id": "invalid-pair",
      "type": "paired_request_loop",
      "pair": [
        { "id": "only" }
      ]
    }
  ],
  "metrics": ["pair.only.elapsed_ms"],
  "aggregations": ["mean"]
}
